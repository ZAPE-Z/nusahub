# Mini Apps Explorer Module

## Purpose
This feature module is structured to accommodate the Mini App Explorer indexing view registry and the secure iframe widget loader container interfaces.

## Directory Structure
- `components/` - Explorer card listings and fullscreen mockup loaders.
- `hooks/` - Registry queries and frame loading triggers.
- `mock/` - Active mini app details mock registry.
- `types/` - TS specifications.
