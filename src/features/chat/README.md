# Chat Feature Module

## Purpose
This feature module is structured to accommodate the Chat conversation lists, bubble logs, and embedded interactive order card widgets in subsequent implementation sprints.

## Directory Structure
- `components/` - Chat lists and active room message views.
- `hooks/` - Messaging hooks.
- `mock/` - Simulated contacts and messaging threads data.
- `types/` - TS specifications.
